name: Build luci-app-kidcontrol for 360T7

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build OpenWrt IPK
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git wget tar xz-utils

    - name: Clone OpenWrt 24.10 source
      run: |
        git clone --depth=1 -b openwrt-24.10 https://git.openwrt.org/openwrt/openwrt.git openwrt

    - name: Copy luci-app-kidcontrol
      run: |
        mkdir -p openwrt/package/luci-app-kidcontrol
        cp -r luci-app-kidcontrol/* openwrt/package/luci-app-kidcontrol/

    - name: Pull OpenWrt SDK Docker
      run: |
        docker pull ghcr.io/openwrt/sdk:mediatek-filogic-24.10-SNAPSHOT

    - name: Build luci-app-kidcontrol inside SDK
      run: |
        docker run --rm \
          -v "${{ github.workspace }}/openwrt:/home/build/openwrt" \
          -w /home/build/openwrt \
          ghcr.io/openwrt/sdk:mediatek-filogic-24.10-SNAPSHOT \
          /bin/sh -c "
            export TERM=dumb &&
            # 编译单包，不用 feeds
            make package/luci-app-kidcontrol/compile -j\$(nproc) V=s
          "

    - name: Generate version tag
      id: tag
      run: |
        VERSION="v24.10-${{ github.run_number }}-$(date +'%Y.%m.%d.%H%M')"
        echo "VERSION=$VERSION" >> $GITHUB_ENV

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ env.VERSION }}
        name: ${{ env.VERSION }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload IPK to Release
      uses: softprops/action-gh-release@v1
      with:
        files: openwrt/bin/packages/*/base/luci-app-kidcontrol_*.ipk
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload IPK as Artifacts (optional)
      uses: actions/upload-artifact@v3
      with:
        name: luci-app-kidcontrol-ipk
        path: openwrt/bin/packages/*/base/luci-app-kidcontrol_*.ipk
